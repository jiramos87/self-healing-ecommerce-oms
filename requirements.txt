# Local dev install. The DEPLOYED dependency set is pyproject.toml
# [project].dependencies, which Vercel installs; pyproject takes precedence
# over this file. The runtime pins below must match it exactly:
# tests/test_packaging.py enforces that.

# Runtime (mirrors pyproject [project].dependencies)
fastapi==0.115.12
langgraph==1.2.8
langchain-core==1.4.9
httpx==0.28.1
psycopg[binary,pool]==3.2.9
pydantic==2.11.7

# Dev tooling only; never deployed.
uvicorn[standard]==0.34.2
pytest==8.4.1
ruff==0.12.1
pyright==1.1.402
